[project]
name = "menu-recommendation"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "slack-bolt",
    "slack-sdk",
    "google-genai",
    "python-dotenv",
]
