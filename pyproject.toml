[project]
name = "menu-recommendation"
version = "0.2.0"
description = "LangGraph 기반 점심 메뉴 추천 Slack 챗봇"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "slack-bolt",
    "slack-sdk",
    "langchain-google-genai",
    "langgraph",
    "python-dotenv",
    "requests",
]
